{{> header }}

<div class="container">
    <div class="row">
        <div class="col-md-8">
            <h1>Archives</h1>
{{#each posts}}
            <h2><a href="{{ this.path }}">{{capitalize this.meta.title}}</a></h2>
{{/each}}
        </div>
          <div class="col-sm-2 col-sm-offset-1 blog-sidebar">
            {{> sidebar }}
          </div>
    </div>
</div>

{{> footer }}
